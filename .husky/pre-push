#!/usr/bin/env sh
# Run full quality gate before allowing a push.
# This enforces ESLint + TypeScript checks to prevent broken lint from reaching main.

npm run check
if [ $? -ne 0 ]; then
  echo "Error: Push aborted because quality checks failed (eslint and/or typecheck)."
  exit 1
fi
