# ESP32 Door Access Configuration
# Agregar estas variables a tu .env.local

# URL del servidor HTTP del ESP32
# Formato: http://{IP}:{PUERTO}
# Ejemplo: http://192.168.1.100:80
ESP32_HTTP_URL=http://192.168.1.100:80

# Token de autenticación del ESP32 (opcional)
# Si el ESP32 valida el header Authorization
# Si no está configurado, se omite el header
ESP32_AUTH_TOKEN=tu-token-secreto-opcional
